# Tensorix credentials — used for open-model text workflows.
TENSORIX_API_KEY=sk-your-tensorix-key-here
TENSORIX_BASE_URL=https://api.tensorix.ai/v1

# OpenRouter credentials — used for VLM workflows (dims_vlm, docs_vlm) and
# closed-model anchors (GPT-5.5, Claude Opus 4.6). Also handles the open VLM ladder.
OPENROUTER_API_KEY=sk-or-v1-your-openrouter-key-here
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1
